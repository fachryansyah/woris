/*
  @param payload Object
  @return true | false (if you got error return false, if success return true)
*/
const startHandler = async (payload) => {

  // destructive param from payload, don't know content of payload ?
  // console.log always helping
  

  return true;
}

module.exports = {
  startHandler
}